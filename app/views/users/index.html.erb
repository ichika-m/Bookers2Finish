<div class="container">
<div class="row" >
<div class="col-md-3">
  <%= flash[:notice] %>
<% if @book.errors.any? %>
<%= @book.errors.count %>errors prohibited this book from being saved:
<ul>
  <% @book.errors.full_messages.each do |message| %>
  <li><%= message %></li>
  <% end %>
</ul>
<% end %>

 <%= render 'books/userinfo', user: @user %>
 <%= render 'books/newbook', book: @book %>
</div>
<br>
<div class="col-md-8 offset-md-1">
<h2>Users</h2>
<table class="table">
  <tr>
    <th>image</th>
    <th>name</th>
    <th colspan ="3"></th>
  </tr>
  <% @users.each do |user| %>
  <tr>
    <td><%= image_tag user.get_profile_image(100, 100) %></td>
    <td><%= user.name %></td>
    <td><%= link_to 'Show', user_path(user.id) %></td><br>
    </tr>
  <% end %>
</table>
</div>
</div>
</div>